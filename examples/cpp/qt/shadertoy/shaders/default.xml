<shadertoy>
    <name>Default</name>
    <fragmentSource>
      <![CDATA[#pragma vr

void main(void)
{
  vec4 color = vec4(iDir, 1.0);
//  vec4 color = texture(iChannel0, iDir); // For cubemaps
//  vec4 color = texture(iChannel0, iDir.xy); // For textures
  gl_FragColor = color; 
}]]></fragmentSource>
</shadertoy>
